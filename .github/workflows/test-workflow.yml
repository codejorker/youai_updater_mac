name: 🧪 测试工作流

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: windows-latest
    
    steps:
    - name: 👋 问候
      run: |
        echo "Hello from Windows!"
        echo "Runner OS: ${{ runner.os }}"
        
    - name: 📦 简单测试
      run: |
        mkdir test-output
        echo "测试成功" > test-output/success.txt
        dir test-output
